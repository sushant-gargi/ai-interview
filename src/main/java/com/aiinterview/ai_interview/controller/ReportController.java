package com.aiinterview.ai_interview.controller;

// This controller has been intentionally removed.
// Reports are accessed exclusively via the role-secured endpoint:
//   GET /api/recruiter/sessions/{id}/report  (RECRUITER only)
// This eliminates an unauthenticated path where any user could guess a session ID and read a report.